import { ArabicDigitsPipe } from './arabic-digits.pipe';

describe('ArabicDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArabicDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
